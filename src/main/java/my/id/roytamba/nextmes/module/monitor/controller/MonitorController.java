package my.id.roytamba.nextmes.module.monitor.controller;

import java.net.URL;
import java.util.ResourceBundle;
import javafx.fxml.Initializable;

public class MonitorController implements Initializable {

    @Override
    public void initialize(URL location, ResourceBundle resources) {
        // Kontainer TabPane untuk modul Monitor.
        // Semua logika tab berada di masing-masing controller (misal: PlcMonitorController).
    }
}
